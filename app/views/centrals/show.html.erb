
<p id="notice"><%= notice %></p>

<p>
  <strong>Room:</strong>
  <%= @central.room_id %>
</p>

<p>
  <strong>Room name:</strong>
  <%= @central.room_name %>
</p>

<%= link_to 'Edit', edit_central_path(@central) %> |
<%= link_to 'Back', centrals_path %>

<div id="draw-map"></div>

<script>
	var map = document.getElementById('draw-map');
	var params = { width: 285, height: 200 };
	var two = new Two(params).appendTo(map);
	var topGroup = two.makeGroup(),
		bottomGroup = two.makeGroup(),
		rect = two.makeRectangle(155, 100, 500, 150),
		circ = two.makeCircle(10, 100, 10, 10);
	circ.fill = "blue";
	rect.fill = "red";

	two.update();
</script>